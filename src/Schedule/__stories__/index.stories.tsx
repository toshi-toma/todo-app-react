import React from "react";

import { storiesOf } from "@storybook/react";
import ScheduleContainer from "../ScheduleContainer";

storiesOf("Schedule", module).add("ScheduleContainer", () => (
  <ScheduleContainer />
));
